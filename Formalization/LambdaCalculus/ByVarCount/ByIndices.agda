-- Examples:
--   (λf. λx. f x) is Abstract(Abstract(Apply(Var 1) (Var 0))) : Term(n)
--   (λx. x) is Abstract(Var 0) : Term(n)
-- Also called: DeBrujin indices.
module Formalization.LambdaCalculus.ByVarCount.ByIndices where
